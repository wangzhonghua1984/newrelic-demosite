replicaCount: 3

image:
  repository: ghcr.io/wangzhonghua1984/newrelic-manager
  tag: ad50b4c55b81200f1c1bc22d0f814d1d70fd806b
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-secret

namespace: newrelic-infra-manager

persistence:
  claimName: infra-manager-data
  mountPath: /app/data
  accessMode: ReadWriteOnce
  size: 1Gi

service:
  name: newrelic-infra-manager-svc
  type: ClusterIP
  port: 80
  targetPort: 8000

ingress:
  className: traefik
  host: infra-manager.jeffstan.cloud
  path: /
  pathType: Prefix

strategy:
  type: RollingUpdate
  maxSurge: 25%
  maxUnavailable: 25%

revisionHistoryLimit: 10
